// import axios from 'axios'

// let options = {
//     timeout: 5000,
//     headers: {
//         post: {
//             'Content-Type': 'application/json'
//         }
//     }
// }
// // 区分test/dev 接口地址
// options.baseURL = (process.env.SERVER === 'aws') ? '' : ''

// export default axios.create(options)